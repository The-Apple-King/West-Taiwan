/*public class warmup4 {
    private ELEM[] data;
    private int length;

    public boolean contains(elem e) {
        for (int i = 0; i < length; i++) {
            if (data[i] == null && e == null) {
                return true;
            } else if (e != null && data[i].equals(e)) {
                return true;
            }

        }
        return false;
    }
}
*/